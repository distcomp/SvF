CVNumOfIter  = 1			# максимальное кол-во итераций поиска весов регуляризации
CVstep       = 21  			# кол-во подмножеств для процедуры кросс-валидации

Select * from  Spring5-small.dat # считывание данных из таблицы 
							

SET:	t = [   ,    , 0.6]  		# область определения функции x(t)
	X = [  -1.5, 1.5, 0.6  ]  		# область значений (с запасом) функции x(t)
 
VAR:    x ( t )   			# искомая функция
	f ( X );  VarType = G #PolyPow = 5 		# искомая правая часть - полином 5-ой степени

EQ:	d2/dt2(x) = f(x)			# дифференциальное ур-ие

OBJ:   	x.MSD() + f.Complexity ( Penal[0] )  # целевая функция – смешанный критерий выбора x(t)

with open('model-d2.txt', 'w') as out_file:
    Task.Gr.pprint(out_file)
    out_file.close()
    
Draw					# отображение всех функции: x(t) и f(x)

